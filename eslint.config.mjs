import base from '@expence-tracker/eslint-config';

/**
 * Корневой конфиг. Приложения переопределяют его своими eslint.config.mjs
 * (`@expence-tracker/eslint-config/next` и `/nest`).
 */
export default base;
